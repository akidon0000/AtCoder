//GIVE ME AC!!!

#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i=0; i<(n);++i)
using ll = long long;
using P = pair<int,int>;
#define _GLIBCXX_DEBUG

int line(int x,int y){
    
}

int main(){
    int N;
    cin >> N;

    int x[N],y[N];
    for (int i=0; i < N; ++i) cin >> x[i] >> y[i];


    rep(i,N) {
        for (int j=a[i]; j<=b[i]; ++j) ans += j;
    }

    cout << ans << endl;
}