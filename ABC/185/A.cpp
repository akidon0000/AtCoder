//GIVE ME AC!!!

#include <bits/stdc++.h>
using namespace std;

#define _GLIBCXX_DEBUG
#define ll = long long;
#define p(x) cout<<x<<endl;
#define rep(i,n) for (int i=0; i<(n);++i)

int a,b,c,d,M,N,K,H,W,L,R,ans=0,counter=0;
// long long a,b,c,M,N,K,H,W,L,R,ans=0,counter=0;

string str;
bool ok=false;


int main(){
    cin >> a>>b>>c>>d;
    ans = min(a,b);
    ans = min(ans,c);
    ans = min(ans,d);
    p(ans)
}